name: Test

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.33.2
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm test:unit
      - run: pnpm build

  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.33.2
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec playwright install --with-deps chromium
      - run: pnpm test:e2e
      - if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 14

  fullstack-e2e:
    if: ${{ vars.FULLSTACK_E2E_ENABLED == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/checkout@v4
        with:
          repository: Apolos-Bible/apolos-backend
          ref: ${{ vars.FULLSTACK_BACKEND_REF || 'main' }}
          token: ${{ secrets.FULLSTACK_REPO_TOKEN }}
          path: backend

      - uses: pnpm/action-setup@v4
        with:
          version: 10.33.2

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, sqlite, pdo_sqlite
          coverage: none

      - name: Prepare isolated Laravel database
        working-directory: backend
        run: |
          composer install --no-interaction --prefer-dist
          npm ci
          cp .env.example .env
          php artisan key:generate
          touch database/fullstack.sqlite
          sed -i "s|^#\?DB_DATABASE=.*|DB_DATABASE=$PWD/database/fullstack.sqlite|" .env
          printf '\nAPP_URL=http://127.0.0.1:8000\nFRONTEND_URL=http://localhost:1420\nQUEUE_CONNECTION=sync\nMAIL_MAILER=array\n' >> .env
          php artisan migrate --force
          npm run build

      - run: pnpm install --frozen-lockfile
      - run: pnpm --dir backend/hocuspocus install --frozen-lockfile
      - run: pnpm exec playwright install --with-deps chromium
      - run: pnpm test:e2e:fullstack
        env:
          APOLOS_BACKEND_DIR: ${{ github.workspace }}/backend
      - if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-fullstack
          path: playwright-report-fullstack/
          retention-days: 14
